import { createBackground } from './Background';

export default {
  title: 'Utility/Background',
  tags: ['autodocs'],
	render: () => createBackground(),
}

/**
 * - `u-bg-*`:`background-color`と`color`を設定するユーティリティ
 * 詳細は、`/src/styles/utils/_bg.scss`をご確認ください。
 */

export const Examples = {}
