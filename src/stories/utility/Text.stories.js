import { createText } from './Text';

export default {
  title: 'Utility/Text',
  tags: ['autodocs'],
	render: () => createText(),
}

/**
 * - `u-lnh-*`:`line-height: *`を設定するユーティリティ
 * - `u-txt-*`:`text-align: *` または色や下線を設定するユーティリティ
 * - `u-fnt-w*`:`font-weight: *`を設定するユーティリティ
 * - `u-fnt-*`:`font-family: *`を設定するユーティリティ
 * - `u-txt-trim--*`:テキストトリミングを設定するユーティリティ
 * 詳細は、`/src/styles/utils/_text.scss`をご確認ください。
 */

export const Examples = {}
