import { Meta } from '@storybook/blocks';

<Meta title="Utility/JS" />

# JS

- `.j-cnt-auto`：`content-visibility: auto` と `contain: content` を適用
- Javascriptから`contain-intrinsic-size` を動的に設定するための目印として使用

詳細は`/src/styles/utils/_js.scss`をご確認ください。
