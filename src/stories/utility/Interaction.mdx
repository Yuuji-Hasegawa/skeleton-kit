import { Meta } from '@storybook/blocks';

<Meta title="Utility/Interaction" />

# Interaction

- `.u-cur-ptr`：`cursor: pointer`を適用
- `.u-hit-exp`:不可視の擬似要素で縦横44pxのヒットゾーンを拡張
- 最小限のタップ領域を確保するためのユーティリティ

詳細は`/src/styles/utils/_interaction.scss`をご確認ください。
