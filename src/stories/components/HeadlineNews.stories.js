import { createHeadlineNews } from './HeadlineNews';

export default {
  title: 'Components/HeadlineNews',
  tags: ['autodocs'],
  render: () => createHeadlineNews(),
}

/**
 * 詳細は、**Show code** または `src/styles/components/_row.scss`をご確認ください。
 * - `.o-sdb`:**Sidebar**オブジェクト
 * - `.c-row__str`:レスポンシブに伸び縮みする子コンポーネント
 */
export const Examples = {}
