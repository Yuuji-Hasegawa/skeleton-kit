import { createUl } from './Ul';

export default {
  title: 'Components/Ul',
  tags: ['autodocs'],
  render: () => createUl(),
}

/**
 * 標準的な**Ul**コンポーネント<br />
 * 詳細は、**Show code** または `src/styles/components/_list.scss`をご確認ください。
 * - `.c-ul`:**Ul**コンポーネント
 * - `.c-entry ul`:`.c-entry`以下の`ul`はセレクタに対して自動でスタイルが割り振られる仕様です。
 */
export const Examples = {}
