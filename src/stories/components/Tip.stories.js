import { createTip } from './Tip';

export default {
  title: 'Components/Tip',
  tags: ['autodocs'],
  render: () => createTip(),
}

/**
 * **Tip**コンポーネント<br />
 * 詳細は、**Show code** または `src/styles/components/_tip.scss`をご確認ください。
 * - `.c-tip`:**Tip**コンポーネント
 */
export const Examples = {}
