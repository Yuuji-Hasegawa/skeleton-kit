import { createBio } from './Bio';

export default {
	title: 'Components/Holder/Bio',
	tags: ['autodocs'],
	render: () => createBio(),
}

/**
 * 著者情報を掲載する**Bio**コンポーネント<br />
 * 詳細は、**Show code** または `src/styles/components/_holder.scss`をご確認ください。
 * - `.c-hld-bio`:**Bio**コンポーネント
 * - `.c-avatar`:**Avatar**コンポーネント
 * - `.c-fld-*`:伸縮する子要素を設定するコンポーネント
 */
export const Examples = {}
