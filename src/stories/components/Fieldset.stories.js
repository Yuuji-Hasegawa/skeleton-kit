import { createFieldset } from './Fieldset';

export default {
  title: 'Components/Fieldset',
  tags: ['autodocs'],
  render: () => createFieldset(),
}

/**
 * **Fieldset**構築イメージ<br />
 * 詳細は、**Show code** または `src/styles/components/_form.scss`をご確認ください。
 * - `.c-frm-*`:**Form**コンポーネント
 */
export const Examples = {}
