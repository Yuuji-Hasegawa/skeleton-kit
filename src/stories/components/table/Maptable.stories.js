import { createMaptable } from './Maptable';

export default {
  title: 'Components/Table/Maptable',
  tags: ['autodocs'],
  render: () => createMaptable(),
}

/**
 * `key:value`型の**Table**コンポーネント<br />
 * 詳細は、**Show code** または `src/styles/components/_table.scss`をご確認ください。
 * - `.c-map-tbl`:**Table**コンポーネント
 * - `.c-map-tbl__key`:**Th**コンポーネント
 * - `.c-map-tbl__val`:**Td**コンポーネント
 */
export const Examples = {}
