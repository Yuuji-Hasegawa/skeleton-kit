import { createReeltable } from './Reeltable';

export default {
  title: 'Components/Table/Reeltable',
  tags: ['autodocs'],
  render: () => createReeltable(),
}

/**
 * **Table**コンポーネント<br />
 * 詳細は、**Show code** または `src/styles/components/_table.scss`をご確認ください。
 * - `.c-rl-tbl`:**Table**コンポーネント
 * - `.c-entry table`:`.c-entry`以下の\`table\`はセレクタに対して自動でスタイルが割り振られる仕様です。
 */
export const Examples = {}
