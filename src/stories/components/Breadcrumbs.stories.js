import { createBreadcrumbs } from './Breadcrumbs';

export default {
  title: 'Components/Breadcrumbs',
  tags: ['autodocs'],
  render: () => createBreadcrumbs(),
}

/**
 * パンくずリスト<br />
 * 詳細は、**Show code** または `src/styles/components/_breadcrumbs.scss`をご確認ください。
 * - `.o-rl`:**Reel**オブジェクト
 * - `.c-bc-item`:**Breadcrumbs**コンポーネントの子要素
 */
export const Examples = {}
