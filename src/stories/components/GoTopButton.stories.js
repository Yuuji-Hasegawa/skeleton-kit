import { createGoTopButton } from './GoTopButton';

export default {
	title: 'Components/GoTopButton',
	tags: ['autodocs'],
	render: () => createGoTopButton(),
}

/**
 * ページ上部へ戻るボタン<br />
 * 詳細は、**Show code** または `src/styles/components/_btn.scss`をご確認ください。
 * - `.c-btn-*`:**Button**コンポーネント
 * - `.grecaptcha-badge`の有無(`body:has(.grecaptcha-badge)`)で位置調整
 * - `.j-back-to-top`:ページ上部へ戻るトリガー
 */
export const Examples = {}
