import { createNote } from './Note';

export default {
  title: 'Components/Note',
  tags: ['autodocs'],
  render: () => createNote(),
}

/**
 * **Note**コンポーネント<br />
 * 詳細は、**Show code** または `src/styles/components/_accessory.scss`をご確認ください。
 * - `.c-note`:**Note**コンポーネント
 */
export const Default = {}
