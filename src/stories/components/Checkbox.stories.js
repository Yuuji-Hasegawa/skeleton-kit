import { createCheckbox } from './Checkbox';

export default {
  title: 'Components/Checkbox',
  tags: ['autodocs'],
  render: () => createCheckbox(),
}

/**
 * **Checkbox**コンポーネント<br />
 * 詳細は、**Show code** または `src/styles/components/_form.scss`をご確認ください。
 * - `.c-frm-chk`:**Checkbox**コンポーネント
 */
export const Examples = {}
