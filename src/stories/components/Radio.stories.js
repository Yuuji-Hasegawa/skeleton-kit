import { createRadio } from './Radio';

export default {
  title: 'Components/Radio',
  tags: ['autodocs'],
  render: () => createRadio(),
}

/**
 * **Radio**コンポーネント<br />
 * 詳細は、**Show code** または `src/styles/components/_form.scss`をご確認ください。
 * - `.c-frm-rdo`:**Radio**コンポーネント
 */
export const Examples = {}
