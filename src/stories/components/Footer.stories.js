import { createFooter } from './Footer';

export default {
  title: 'Components/Footer',
  tags: ['autodocs'],
  render: () => createFooter(),
}

/**
 * **Footer**コンポーネント<br />
 * 詳細は、**Show code** または `src/styles/components/_footer.scss`をご確認ください。
 *
 * - `.c-gnd`:**Footer**コンポーネント
 * - `.c-fld-*`:伸縮する子要素を設定するコンポーネント
 */
export const Examples = {}
