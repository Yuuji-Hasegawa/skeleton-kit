import { createRadius } from './Radius';

export default {
  title: 'Tokens/Radius',
  tags: ['autodocs'],
  render: () =>createRadius(),
}

/**
 * `map.get($borderRadius, $key)`で`$borderRadius`マップに格納された値を取得可能
 *
 * 詳細は、`/src/styles/settings/_sizes.scss`をご確認ください。
 * - `$borderRadius(key: value)`:`borderRadius`を格納したマップ
 */

export const Examples = {}
